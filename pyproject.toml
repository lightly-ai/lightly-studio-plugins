[project]
name = "lightly-studio-plugins"
version = "0.1.0"
description = "All Lightly Studio plugins"
requires-python = ">=3.8"
dependencies = [
]

[tool.uv.sources]

[tool.uv.workspace]
members = ["plugins/*"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
