[project]
name = "lightly_plugins_kitti_export"
version = "0.1.0"
description = "KITTI object detection export plugin for Lightly Studio"
requires-python = ">=3.10"
dependencies = [
    "lightly_studio>=1.0.0",
    "labelformat",
    "sqlmodel",
]

[project.entry-points."lightly_studio.plugins"]
kitti_export = "lightly_plugins_kitti_export.operator:ExportKittiOperator"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lightly_plugins_kitti_export"]
